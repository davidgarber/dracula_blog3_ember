

  <h1>contacts</h1>
  <ul>
    <li>{{#link-to 'phone'}}Phone{{/link-to}}</li>
    <li>{{#link-to 'email'}}Email{{/link-to}}</li>
  </ul>

  {{#if messageSent}}
  <p>
    Thank you. meesage sent.
  </p>
  {{confirmationNumber}}
  {{else}}
  <p>Or, <a href="#" {{action 'sendMessage'}}>click here</a> to send a secret message.</p>
  {{outlet}}
  {{/if}}
